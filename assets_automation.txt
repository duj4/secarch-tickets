\copy (
    SELECT
        r."ID",
        r."NAME",
        r."SCHEMA_ID",
        s."NAME" AS "SCHEMA_NAME",
        r."ACTOR_USER_KEY",
        r."DISABLED",
        r."CREATED",
        r."UPDATED"
    FROM public."AO_8542F1_IFJ_ATM_RULE" r
    LEFT JOIN public."AO_8542F1_IFJ_OBJ_SCHEMA" s
      ON s."ID" = r."SCHEMA_ID"
    ORDER BY s."NAME", r."NAME"
) TO '/tmp/assets_automation_before_disable.csv'
WITH CSV HEADER;

SELECT
    "DISABLED",
    count(*)
FROM public."AO_8542F1_IFJ_ATM_RULE"
GROUP BY "DISABLED"
ORDER BY "DISABLED";

SELECT count(*) AS enabled_assets_automation
FROM public."AO_8542F1_IFJ_ATM_RULE"
WHERE "DISABLED" IS DISTINCT FROM true;


